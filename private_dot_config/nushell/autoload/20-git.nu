# Git aliases

if (which git | is-empty) { return }

alias g = git
alias gs = git status
alias ga = git add
alias gc = git commit
alias gd = git diff
alias gl = git log --oneline -20
alias gp = git push
