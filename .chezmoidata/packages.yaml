packages:
  arch:
    # Personal roles layered on top of a CachyOS Niri installation.
    # The distribution remains responsible for the system and desktop baseline.
    enabledRoles:
      - base
      - desktop
      - gaming

    roles:
      base:
        repo:
          - age
          - bat
          - btop
          - chezmoi
          - eza
          - fd
          - fish
          - fzf
          - git-delta
          - github-cli
          - just
          - jujutsu
          - lazygit
          - mergiraf
          - neovim
          - podman
          - ripgrep
          - starship
          - uv
          - yay
          - yazi
          - zoxide
        aur:
          - carapace-bin
          # Kept for the systemd unit and the binary's fallback; the pinned
          # upstream build below is what the service actually runs, because the
          # chords-v2 fix home-row modifiers need is only in a prerelease.
          - kanata
          - passage-git
        binaries:
          - script: kanata.sh
            version: 1.12.1-prerelease-1
            system: true

      desktop:
        repo:
          - kitty
          - ttf-jetbrains-mono-nerd
          - wtype
          - zen-browser-bin
        aur: []
        binaries: []

      gaming:
        repo:
          - cachyos-gaming-meta
          - cachyos-gaming-applications
        aur: []
        binaries: []

      # Enable per machine when provisioning a remote/server environment.
      server:
        repo: []
        aur: []
        binaries:
          - script: zmx.sh
            version: 0.7.0
